import { ObtenerFechaPipe } from './obtener-fecha.pipe';

describe('ObtenerFechaPipe', () => {
  it('create an instance', () => {
    const pipe = new ObtenerFechaPipe();
    expect(pipe).toBeTruthy();
  });
});
