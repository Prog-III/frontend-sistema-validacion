import { ExisteImagenPipe } from './existe-imagen.pipe';

describe('ExisteImagenPipe', () => {
  it('create an instance', () => {
    const pipe = new ExisteImagenPipe();
    expect(pipe).toBeTruthy();
  });
});
